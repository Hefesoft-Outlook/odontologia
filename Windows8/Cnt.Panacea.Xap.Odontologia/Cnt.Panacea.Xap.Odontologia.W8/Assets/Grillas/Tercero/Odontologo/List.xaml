<UserControl
    x:Class="App2.Assets.Grillas.Tercero.Odontologo.List"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"        
    xmlns:Assets="using:Cnt.Panacea.Xap.Odontologia.Assets"
    xmlns:local="using:App2.Assets.Grillas.Diagnosticos_procedimientos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    
	DataContext="{Binding Odontologo, Mode=OneWay, Source={StaticResource Locator}}"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <DataTemplate x:Key="DataTemplate">
            <Grid>
                <TextBlock Text="{Binding NombreCompleto}"></TextBlock>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <GridView 
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch"
            ItemTemplate="{StaticResource DataTemplate}"
            ItemsSource="{Binding Listado}" 
            SelectedItem="{Binding Seleccionado, Mode=TwoWay}"/>
    </Grid>
</UserControl>
