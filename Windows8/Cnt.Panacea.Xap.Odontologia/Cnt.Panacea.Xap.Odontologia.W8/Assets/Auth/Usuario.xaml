<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App2.Auth"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    
    x:Class="App2.Auth.Usuario"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" 
    DataContext="{Binding Usuarios, Source={StaticResource Locator}}">    

    <Grid>
        <StackPanel>
            
            <Button Background="White">
                <Image Width="60" Source="{Binding UsuarioActivo.imagenRuta}"/>

                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem 
                            Command="{Binding changeUserCommand}"
                            x:Name="MnFlytItmCambiarUsuario"                            
                            Text="Cambiar de usuario"/>
                        <MenuFlyoutSeparator/>                        
                    </MenuFlyout>
                </Button.Flyout>

            </Button>
            
            
            
            <TextBlock Text="{Binding UsuarioActivo.nombre}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
    
</UserControl>
